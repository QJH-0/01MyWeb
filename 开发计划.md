# MyWeb 开发计划文档（重构重开版）

**版本**: v2.1  
**日期**: 2026-03-26  
**依据**: `需求文档.md`、`tech-spec.md`、`docs/开发文档/`*  
**目标**: 从框架重整开始，按模块闭环开发，确保每一步可测试、可提交、可复现。  
**当前阶段**: `M0`（基础框架与联调链路）- 进行中

---

## 1. 总体执行原则（强制）

1. 一次只推进一个主模块，不跨模块并行开发。
2. 每个模块必须完成完整闭环：`任务拆分 -> 实现 -> 自测 -> 联调 -> 回归 -> 文档更新 -> Git 提交`。
3. 测试未通过，不允许切入下一模块。
4. 禁止硬编码密钥，所有敏感配置仅允许环境变量注入。
5. 每次提交都必须保证其他成员可在新环境复现（启动、测试、关键流程可跑通）。

---

## 2. 统一完成定义（DoD）

每个模块完成时必须同时满足以下条件：

- 功能完成：模块范围内所有功能点全部实现，且无已知阻断缺陷。
- 测试完成：单元/集成测试通过，关键路径有可重复执行的验证结果。
- 联调完成：前后端核心流程走通（列表、详情、创建、更新、删除、查询）。
- 回归完成：已完成功能无明显回退（至少执行一次冒烟回归）。
- 文档完成：`开发计划.md` 与对应阶段文档（`docs/开发文档/0x-*.md`）以及验收闸门（`docs/开发文档/99-阶段验收清单.md`）同步更新；如本轮状态有变化，同步更新 `00E-执行总控看板（唯一入口）.md`。
- 提交完成：按 Conventional Commits 提交到本地仓库，提交信息明确变更目的。

---

## 3. 统一测试与提交流程（每模块必做）

### 3.1 测试流程

1. 后端测试：执行 `mvn test`，要求全量通过。
2. 前端构建：执行 `npm run build`，要求构建通过。
3. 核心手测：对本模块关键用户路径执行至少 1 轮人工验证。
4. 边界验证：至少覆盖成功、失败、边界参数三类输入。
5. 回归验证：验证首页、导航、健康检查、已完成核心模块不受影响。

### 3.2 Git 提交流程

1. 仅暂存本模块相关文件。
2. 确认无敏感信息后提交。
3. 提交信息使用 Conventional Commits：
  - `feat(module): ...`
  - `fix(module): ...`
  - `refactor(module): ...`
  - `test(module): ...`
  - `docs(module): ...`

---

## 4. 可复现标准（项目级）

项目必须长期满足以下复现标准：

1. 依赖服务可一键启动：`docker compose up -d`。
2. 前后端可按文档命令独立启动。
3. 健康检查接口可用，关键 API 可调用。
4. 测试命令稳定通过（后端测试 + 前端构建）。
5. 新人可在空环境按 README/文档在 30 分钟内跑通核心链路。

---

## 5. 总体阶段计划（M0 -> M8）

1. `M0` 基础框架与工程治理重整
2. `M1` 内容展示模块
3. `M2` 项目管理与展示模块
4. `M3` 博客管理与展示模块
5. `M4` 搜索模块
6. `M5` 文件管理模块
7. `M6` AI 助手与 RAG 模块
8. `M7` 动效与体验优化模块
9. `M8` 生产部署与上线收口模块

---

## 6. 阶段文档索引（唯一细节来源）

本文件只保留“总纲 + 进度”，不再承载各阶段的细化清单（避免与 `docs/开发文档/01~08` 重复/冲突）。  
各阶段的**功能清单、测试证据、验收勾选**统一以阶段文档与 `99-阶段验收清单.md` 为准。

- **执行入口**：`docs/开发文档/00E-执行总控看板（唯一入口）.md`
- **开发铁律**：`docs/开发文档/00C-最终压缩执行版（AI只读此文档先行）.md`
- **类设计与方法签名**：`docs/开发文档/00D-全模块实现方案与类设计蓝图.md`
- **接口契约唯一来源**：`docs/开发文档/API文档与系统架构.md`
- **阶段闸门**：`docs/开发文档/99-阶段验收清单.md`
- **阶段文档（只读当前阶段 1 份）**：
  - `docs/开发文档/01-M0-基础框架与联调链路.md`
  - `docs/开发文档/02-M1-内容展示模块.md`
  - `docs/开发文档/03-M2-项目管理与展示模块.md`
  - `docs/开发文档/04-M3-博客管理与展示模块.md`
  - `docs/开发文档/05-M4-M5-搜索与文件管理模块.md`
  - `docs/开发文档/06-M6-AI助手与RAG模块.md`
  - `docs/开发文档/07-M7-动效与体验优化模块.md`
  - `docs/开发文档/08-M8-生产部署与上线手册.md`

---

## 7. 文档更新约定（减少重复）

- **每轮结束必更**：当前阶段文档（`01~08`）+ `99-阶段验收清单.md` + `00E`（仅当阶段/状态变更）。
- **本文件只更新**：第 10 节“当前执行状态”（与 `00E` 保持一致）以及必要的索引链接（如新增/改名）。
- **分块/恢复/回滚**：统一按 `00E` 第 6~8 节执行（Token 分块协议、会话快照、Git 安全回滚）。

---

## 8. 分阶段实施文档（执行入口）

为便于“按顺序推进直到上线”，本计划对应拆分文档如下（必须按顺序执行）：

1. `docs/开发文档/00E-执行总控看板（唯一入口）.md`
2. `docs/开发文档/01-M0-基础框架与联调链路.md`
3. `docs/开发文档/02-M1-内容展示模块.md`
4. `docs/开发文档/03-M2-项目管理与展示模块.md`
5. `docs/开发文档/04-M3-博客管理与展示模块.md`
6. `docs/开发文档/05-M4-M5-搜索与文件管理模块.md`
7. `docs/开发文档/06-M6-AI助手与RAG模块.md`
8. `docs/开发文档/07-M7-动效与体验优化模块.md`
9. `docs/开发文档/08-M8-生产部署与上线手册.md`
10. `docs/开发文档/99-阶段验收清单.md`（阶段闸门，必须逐阶段验收）

补充日程执行清单（按天推进）：

- 已移除：日程清单容易与阶段清单重复/冲突，本仓库以 `00E` + 当前阶段文档（`01`~`08`）+ `99` 作为唯一执行入口与验收依据。

---

## 9. 统一口径（当前版本）

- **执行入口**：`docs/开发文档/00E-执行总控看板（唯一入口）.md`
- **开发铁律**：`docs/开发文档/00C-最终压缩执行版（AI只读此文档先行）.md`
- **类设计与方法签名**：`docs/开发文档/00D-全模块实现方案与类设计蓝图.md`
- **接口契约唯一来源**：`docs/开发文档/API文档与系统架构.md`
- **技术选型与非功能规范**：`tech-spec.md`

---

## 10. 当前执行状态（与 00E 同步）

- **当前阶段**：`M0`（基础框架与联调链路）
- **状态**：进行中
- **最后更新**：2026-03-26
- **详细进度**：见 `docs/开发文档/00E-执行总控看板（唯一入口）.md`

---

**文档版本**：v2.1  
**最后更新**：2026-03-26

