# MyWeb 开发计划文档（分模块迭代版）

**版本**: v1.1  
**日期**: 2026-03-24  
**依据**: `需求文档.md`、`tech-spec.md`

---

## 1. 目标与执行原则

本计划遵循“**先搭整体框架，再按模块逐个实现**”的开发方式，避免一次性并行开发所有功能导致联调失控。

- 先完成前后端基础框架与联调链路，确保系统可启动、可访问、可部署。
- 按模块顺序迭代开发，每次只推进一个主模块（可含少量配套子功能）。
- 每个模块完成后必须执行测试，测试通过后再进行 Git 提交。
- 模块未通过测试不得进入下一个模块开发。

---

## 2. 模块定义（按业务与技术分层）

## 2.1 基础框架模块（M0）

- 前端工程骨架：Vue3 + Vite + Router + Pinia + Axios + SCSS 变量体系
- 后端工程骨架：Spring Boot 3.5.12 + Java 21 + 分层目录结构（controller/service/repository 等）
- 基础中间件连接：MySQL / Redis / Elasticsearch / MinIO（先连通，后深化）
- 基础部署与反向代理：Docker Compose + Nginx
- 基础规范：统一响应体、错误码、环境变量、日志、CORS、安全基础配置

## 2.2 内容展示模块（M1）

- 首页（`/`）：简介、精选项目、最新博客入口、AI 入口占位
- 关于我（`/about`）：个人介绍、技能标签、统计信息
- 经历时间线（`/experience`）：时间轴数据展示
- 联系方式（`/contact`）：静态联系方式 + 表单框架

## 2.3 项目管理与展示模块（M2）

- 前台：项目列表、筛选、详情展示、外链跳转
- 后台：项目 CRUD（含封面、技术栈、简介、链接）
- 文件引用：项目封面与附件通过对象存储 URL 引用

## 2.4 博客管理与展示模块（M3）

- 前台：博客列表、分页、分类/标签筛选、详情页（Markdown 渲染）
- 后台：博客 CRUD、草稿/发布、基础编辑能力
- 阅读信息：发布日期、标签等元信息展示

## 2.5 搜索模块（M4）

- Elasticsearch 全文索引建立
- 博客/项目写入双写（MySQL 权威 + ES 最终一致）
- 搜索接口（关键词检索 + 高亮 + 结果分页）
- Outbox/重试/告警与索引重建入口（至少保留管理入口或脚本）

## 2.6 文件管理模块（M5）

- 文件上传（图片/Markdown/文本）
- MinIO 存储与访问 URL 生成
- 文件列表、删除、业务引用关系管理
- 严格遵守 S3 兼容契约，避免绑定 MinIO 私有语义

## 2.7 AI 助手与 RAG 模块（M6）

- DashScope 对话能力接入（Spring AI Alibaba）
- SSE 流式对话接口（`delta/done/error`）
- 向量索引构建（Embedding 固定 768 维，独立向量索引）
- 混合检索（向量 + BM25）与 RRF 融合
- 引用回溯（`sourceType/sourceId/title/url`）与会话存储（Redis + MySQL）

## 2.8 动效与体验优化模块（M7）

- 路由过渡、滚动动画、卡片悬浮等轻量动画
- 响应式和移动端适配
- 性能优化（懒加载、资源压缩、接口性能基线）

## 2.9 生产部署与运维模块（M8）

- 生产环境部署脚本与配置
- HTTPS、鉴权、限流、上传校验、安全基线
- 监控告警、日志留存、资源评估（2C4G 风险与 ES 拆分策略）

---

## 3. 开发顺序（严格执行）

开发顺序采用“主干优先 + 单模块闭环”：

1. `M0` 基础框架模块（必须先完成）
2. `M1` 内容展示模块
3. `M2` 项目管理与展示模块
4. `M3` 博客管理与展示模块
5. `M4` 搜索模块
6. `M5` 文件管理模块
7. `M6` AI 助手与 RAG 模块
8. `M7` 动效与体验优化模块
9. `M8` 生产部署与运维模块

说明：

- `M2` 与 `M3` 完成后再做 `M4`，可保证搜索接入有真实数据来源。
- `M5` 在 `M2/M3` 后推进，可直接服务项目/博客内容引用。
- `M6` 放在 `M4/M5` 后，因其依赖内容数据、全文检索与文件引用链路。

---

## 4. 每个模块的标准执行流程（DoD）

每个模块都必须按以下流程推进，不可跳步；统一采用 `任务拆分 -> 实现 -> 自测 -> 联调 -> 回归 -> 提交`：

1. **任务拆分**：将模块拆成 2~5 个可落地功能点（接口、页面、数据、联调）。
2. **实现**：仅实现当前模块范围，不引入下个模块逻辑。
3. **本地自测**：
   - 前端：页面可访问、主要交互可用、无阻断报错。
   - 后端：接口可调用、参数校验有效、异常响应符合统一格式。
   - 数据层：MySQL/ES/Redis/MinIO 相关操作可执行且结果正确。
4. **联调验证**：前后端联调通过，关键路径走通（列表、详情、提交、查询等）。
5. **回归检查**：不影响已完成模块（至少冒烟验证首页/导航/核心接口）。
6. **Git 提交**：仅在测试通过后提交，提交信息需体现模块与变更目的。

---

## 5. 测试与提交流程（强制）

## 5.1 模块完成后的最低测试清单

- 启动验证：前端可启动、后端可启动、依赖服务可用。
- 功能验证：本模块核心功能逐条通过。
- 接口验证：成功、失败、边界参数至少各 1 组。
- 日志验证：无持续报错、无明显异常堆栈。
- 数据验证：写入/更新/删除后数据与索引一致性符合预期（允许最终一致的模块需确认重试路径）。

## 5.2 Git 提交规则

- 原则：**先测后提，未通过不提交**。
- 建议粒度：一个提交对应一个可验证功能点或一个小型子模块。
- 提交前检查：
  - 代码可运行
  - 测试通过
  - 无敏感信息（密钥、密码）入库
- 提交信息建议模板：
  - `feat(module): add xxx capability`
  - `fix(module): resolve xxx issue`
  - `refactor(module): simplify xxx logic`

---

## 6. 里程碑安排（滚动排期）

> 以下排期按“当前进度从 2026-03-24 起算”，采用保守估计，确保单模块闭环推进。

- Week 1（当前周）：完成 `M2` 收口（后台管理联调、边界测试、回归）。
- Week 2：`M3` 博客管理与展示模块（后端 CRUD + 前台列表/详情 + 发布流）。
- Week 3：`M4` 搜索模块 + `M5` 文件管理模块（先契约与索引，再文件链路）。
- Week 4~5：`M6` AI 助手与 RAG 模块（SSE、向量检索、混合检索、会话存储）。
- Week 6：`M7` 动效与体验优化 + `M8` 生产部署与上线准备。

---

## 7. 风险与控制

- **风险 1（并行过多）**：同时做多个模块导致问题定位困难。  
  **控制**：严格“一次一个主模块”，最多并行处理同模块内前后端配套。

- **风险 2（测试缺失）**：功能看似完成但不可运行。  
  **控制**：执行第 5 节测试清单，不通过不进入下一模块。

- **风险 3（2C4G 资源紧张）**：ES + JVM 占用过高。  
  **控制**：开发环境保守参数，生产优先升配或 ES 独立部署。

- **风险 4（AI 成本与稳定性）**：Embedding 与对话调用费用不可控。  
  **控制**：限制 topK、控制切块数量、增加缓存与限流。

---

## 8. 本计划执行口径（最终约束）

- 不做“大爆发式”全量开发。
- 永远先构建系统骨架，再做业务模块。
- 每完成一个功能就测试；测试通过后立即 Git 提交。
- 所有模块按顺序闭环推进，确保每一步都可运行、可验证、可回滚。

---

## 9. 分阶段实施文档（执行入口）

为便于“按顺序推进直到上线”，本计划对应拆分文档如下（必须按顺序执行）：

1. `docs/开发文档/00-开发文档导航.md`
2. `docs/开发文档/01-M0-基础框架与联调链路.md`
3. `docs/开发文档/02-M1-内容展示模块.md`
4. `docs/开发文档/03-M2-项目管理与展示模块.md`
5. `docs/开发文档/04-M3-博客管理与展示模块.md`
6. `docs/开发文档/05-M4-M5-搜索与文件管理模块.md`
7. `docs/开发文档/06-M6-AI助手与RAG模块.md`
8. `docs/开发文档/07-M7-动效与体验优化模块.md`
9. `docs/开发文档/08-M8-生产部署与上线手册.md`

补充日程执行清单（按天推进）：

1. `docs/开发文档/11-M0-日程执行清单.md`
2. `docs/开发文档/12-M1-日程执行清单.md`
3. `docs/开发文档/13-M2-日程执行清单.md`
4. `docs/开发文档/14-M3-日程执行清单.md`
5. `docs/开发文档/15-M4-M5-日程执行清单.md`
6. `docs/开发文档/16-M6-日程执行清单.md`
7. `docs/开发文档/17-M7-日程执行清单.md`
8. `docs/开发文档/18-M8-日程执行清单.md`

---

## 10. 文档差异与修订记录（2026-03-24）

本节用于记录“需求文档 / 技术规范 / 开发计划 / 开发文档导航”的同步结果，确保后续执行仅遵循一套口径。

### 10.1 本轮发现的执行冲突（已对齐）

1. **执行流程口径分散**：DoD 在多个文档中有表达，但缺少统一锚点。  
   **对齐结果**：统一采用 `任务拆分 -> 实现 -> 自测 -> 联调 -> 回归 -> 提交`。
2. **测试门槛表达不集中**：有“未通过测试不得跳阶段”，但缺少统一入口说明。  
   **对齐结果**：导航文档补充执行入口，默认先阶段版再日程版，模块闭环后再进入下一阶段。
3. **技术参数与边界分布在多处**：版本、RAG 参数、安全边界分散于需求与技术规范。  
   **对齐结果**：需求文档补“统一完成定义”，技术规范补“执行口径映射”，并明确开发/生产边界。

### 10.2 统一执行入口（新增约束）

- 先读 `docs/开发文档/00-开发文档导航.md`，确认当前阶段编号。
- 每个阶段先完成阶段文档，再按对应日程清单逐天推进。
- 每完成一个功能点必须本地验证；通过后才允许提交。
- 阶段 DoD 未通过，不得切入下一阶段。

### 10.3 统一技术基线（执行时以此为准）

- 前端：Vue `3.5.30`、Vite `8.0.2`、Pinia `3.0.4`、Axios `1.13.6`。
- 后端：Java `21`、Spring Boot `3.5.12`。
- 检索与 AI：Elasticsearch `8.19.13`、Spring AI Alibaba `1.1.2.2`。
- RAG 默认参数：Vector `topK=6`、`similarityThreshold=0.75`，BM25 旁路 `topK<=4`，融合采用 RRF，切块目标约 `500 tokens`、overlap `10%-20%`。
- 安全边界：生产必须 HTTPS、密钥环境变量注入、ES 认证与 TLS 开启。

---

## 11. 当前开发进度快照（2026-03-24）

### 11.1 模块状态

- `M0`：已完成（前后端骨架、依赖服务联通、统一响应与健康检查已落地）。
- `M1`：已完成（首页/关于/经历/联系页面可访问，导航与基础交互可用）。
- `M2`：进行中（后端项目 CRUD、前台项目页、后台项目页已具备；待收口联调与回归）。
- `M3`~`M8`：未开始（按顺序推进，不并行跨模块）。

### 11.2 M2 收口任务（进入 M3 前必须完成）

1. 统一完成 `M2` 成功/失败/边界参数测试记录。
2. 补齐后台项目管理分页与筛选联调验证记录。
3. 完成一次 M1+M2 冒烟回归，并在开发文档标记 DoD。
4. 提交 `feat(m2)` 收口提交后，方可切入 `M3`。

