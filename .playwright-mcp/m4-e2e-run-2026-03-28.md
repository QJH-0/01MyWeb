# M4 E2E 执行摘要（2026-03-28）

## 环境

- `docker compose up -d mysql redis elasticsearch minio`
- 后端 `mvn spring-boot:run`（8080）；前端 `npm run dev`（5173）
- 关键词：`M4E2EUniqueToken2847`；测试博客 slug：`m4-e2e-unique-2847`（已删除）

## 步骤与留证

1. Playwright MCP：管理员登录 → 新建博客 → 保存 → 列表 **发布**。
2. API：`POST /api/admin/search/consume`×2，`data.processed=0`（outbox 已由 `SearchOutboxScheduler` 消费空队列；若仍有待处理会 `processed>0`）。详见 `.playwright-mcp/m4-e2e-consume-search-2026-03-28.txt`。
3. `GET /api/search?q=M4E2EUniqueToken2847`：`list` 含 blog#3，`highlights` 含 `<mark>M4E2EUniqueToken2847</mark>`。
4. Playwright：`/search?q=...` 快照见 `<mark>` 高亮；截图 `m4-e2e-search-hit-highlight-2026-03-28.png`；网络 `m4-e2e-search-network-2026-03-28.log`。
5. Playwright：博客列表 **删除** 该文 → 再次检索，`/search` 为「无匹配结果、共 0 条」；截图 `m4-e2e-search-after-delete-2026-03-28.png`。
6. API：删除后 `GET /api/search?...` 的 `total=0`。

## 门禁（本轮）

- `mvn -B test`：BUILD SUCCESS
- `npm run build`：成功
