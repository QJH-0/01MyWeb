# M0 基础框架与联调链路（细化版）

> 约束：本文件仅定义 M0 执行清单；接口字段、响应结构、鉴权与分页口径统一以 `API文档与系统架构.md` 为准。

## 1. 模块目标

建立稳定可运行、可测试、可复现的项目基线，彻底解决环境不可复现、链路不稳定、页面空白的根因问题。

## 2. 本阶段任务拆分（只做 1 个小任务/回合）

说明：M0 只关注“工程可跑通 + 可验证”，不做业务扩展。每回合只选下面 1 个小任务闭环（遵循 `00C`），完成后把证据回填到 `99-阶段验收清单.md` 的 M0 区域。

- 任务 1：配置分层与环境模板（后端 `application*.yml` + 前端 `.env*` 占位）
- 任务 2：中间件连通与健康探针聚合（MySQL/Redis/ES/MinIO）
- 任务 3：统一响应 + 统一异常 + traceId + CORS 全链路生效
- 任务 4：JWT 认证与权限边界（register/login/me 为最小闭环；其他按契约补齐）
- 任务 5：前端联调展示健康状态 + 登录页（`/login`）最小可用 + Nginx 反代与 SPA 回退
- 任务 6：测试补齐与阶段收口（`mvn test` / `npm run build` + 99 勾选 + 文档快照）

## 3. 接口与契约（只引用，不在此重复）

### 3.1 健康检查接口（唯一契约引用）

- 接口契约：以 `API文档与系统架构.md` 的 `GET /api/health` 为准（禁止在本文件重复定义响应字段/错误字段）。
- 本阶段要求：前端可稳定调用并展示健康状态；后端保证 traceId 透传/生成链路可用。
  - 当 `data.elasticsearch=false` 时，`data.elasticsearchStatus` 固定为 `UNAVAILABLE`；当可用时返回集群健康状态（green/yellow）。

## 4. 配置与环境变量（只保留最小口径）

- **前端**：最少只需要 `VITE_API_BASE_URL`、`VITE_APP_TITLE`（其余按项目实际补充）。
- **后端密钥**：一律通过环境变量注入（禁止入库）；具体键名以 `application*.yml` 为准。

## 6. 测试计划

### 6.1 自动化测试

- 后端：至少 1 个启动测试 + 1 个健康检查集成测试。
- 后端：至少 1 个认证接口集成测试（register/login/me 任一路径即可；refresh/logout 不作为本阶段强制验收）。
- 后端：开发启动验证通过（`mvn spring-boot:run`，默认 `dev` profile 使用 MySQL）。
- 前端：构建校验通过（`npm run build`）。

### 6.2 手工验证

- `docker compose up -d` 后依赖服务全部可用。
- 前后端独立启动成功。
- 前端打开后能看到后端健康状态。
- 访问 `/login` 页面可用，且可完成最小登录联调（成功/失败反馈）。

## 7. 验收与证据（不在此逐项重复）

- **验收打勾**：统一在 `99-阶段验收清单.md` 的 `M0` 区域完成。
- **证据要求（最小）**：
  - `mvn -B test`：全量通过（或明确列出允许跳过的测试范围与原因）
  - `mvn spring-boot:run`：开发启动成功（默认 `dev` profile，MySQL 可连通）
  - `npm run build`：构建成功
  - 联调说明：至少 1 条可复现方式（`curl/Swagger/前端页面`）+ 结果

## 8. DoD（完成定义）

- 基础链路稳定，首次拉取仓库可在 30 分钟内跑通。
- 统一响应、异常、日志、CORS 全部生效。
- 基础鉴权与认证链路稳定可用。
- 通过 M0 测试门禁后方可进入 M1。

## 8A. 约束引用（固定）

- 本阶段“验证方式 / 禁止项 / 提交要求”统一执行 `00C-最终压缩执行版（AI只读此文档先行）.md`，不在本文件重复定义。

---

## 9. 阶段执行卡（预填）

- 阶段：`M0`
- 负责人：``
- 状态：`已完成`
- 开始时间：`2026-03-27`
- 更新时间：`2026-03-27`

### 9.1 本阶段目标

- 建立可运行、可测试、可复现的基础联调链路。

### 9.2 本阶段任务

- [x] 完成配置分层与环境模板
- [x] 完成 MySQL/Redis/ES/MinIO 连通探针聚合（`/api/health` 实时探测）
- [x] 完成统一响应/异常/traceId/CORS
- [x] 打通 `GET /api/health` 与前端展示
- [x] 完成 JWT 认证与权限边界（`register/login/refresh/logout/me` + `/api/admin/**` 与 `/api/ai/**` 鉴权）
- [x] 完成登录页（`/login`）最小可用联调（调用登录接口并反馈结果）
- [x] 补齐认证细粒度错误码（后端返回 + 前端映射）：`AUTH_INVALID_CREDENTIALS`、`AUTH_INVALID_REFRESH_TOKEN`、`AUTH_USERNAME_EXISTS` 等

### 9.3 测试证据

- 后端：`mvn -B test` -> `PASS（Tests run: 19, Failures: 0, Errors: 0）`
- 后端：`mvn spring-boot:run` -> `PASS（默认 dev profile，Tomcat 8080 启动成功，MySQL 连接成功）`
- 前端：`npm run build` -> `PASS`
- 联调说明：详见 `docs/开发文档/00E-执行总控看板（唯一入口）.md` 的“快速回填区”。

### 9.4 DoD 结论

- `M0` 验收结论已在 `99-阶段验收清单.md` 勾选为通过，允许进入 `M1`。
- 本文件仅记录 M0 差异项与证据。
