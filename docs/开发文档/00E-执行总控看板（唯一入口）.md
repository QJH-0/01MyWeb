# 00E 执行总控看板（唯一入口）

> 本文档是执行入口，不承担详细规范。  
> 详细规则仍以 `00C`、`00D`、`API文档与系统架构`、以及 `错误码与错误响应` 为准。

## 0. 模型开发起读顺序（固定，尽量少读）

1. 先看本页“当前阶段”。
2. 再读 `00C-最终压缩执行版（AI只读此文档先行）.md`（铁律）。
3. 再读“当前阶段文档”（`01~08` 仅 1 份）。
4. **仅当遇到不确定/冲突时**才按需补读：`00D`（索引） + 对应阶段的 `00D-Mx` / `API文档与系统架构.md` / `错误码与错误响应.md`。
5. 收口时只更新：当前阶段文档 + `99-阶段验收清单.md` + 本页“快速回填区”。

## 1. 当前阶段

- 当前阶段：`M1`
- 当前目标：完成 M1 首个可验收小任务闭环（含降级提示），并更新 `99-阶段验收清单.md`
- 当前状态：`M1 进行中`
- 最后更新时间：`2026-03-27`

## 2. 本轮“默认只读”文档（3 份）

1. 本页（`00E`）
2. `00C-最终压缩执行版（AI只读此文档先行）.md`
3. 当前阶段文档（M0~M8 仅 1 个）

说明：`00D`（索引）/ `00D-Mx` / `API` / `错误码` / `99` 都是 **按需读取**：只有在“出现不确定/冲突/准备验收收口”时才打开。

## 3. 本轮执行模板

- 阶段：`Mx`
- 目标：`一句话目标`
- 涉及模块：`backend/frontend/docs`
- 涉及接口：`/api/...`
- 风险点：`最多 3 条`
- 禁止事项：`不跨阶段、不改未声明契约、不跳过测试`

## 4. 阶段闸门（全部通过才可进入下一阶段）

- [ ] 代码实现完成（仅本阶段范围）
- [ ] 自动化通过（后端 `mvn -B test` + `mvn spring-boot:run` / 前端 `npm run build`）
- [ ] 联调通过（主链路可走通）
- [ ] 前端具备后端不可用降级（默认数据渲染 + “后端连接失败”可见提示，写操作不得误报成功）
- [ ] 文档已更新（阶段文档 + `99-阶段验收清单.md`）
- [ ] 无新增未记录风险

## 5. 快速回填区（每轮结束填写）

- 完成项：
  - `M0 已完成前后端工程初始化（Vue3+Vite+Router+Pinia / Spring Boot 3.5.12）。`
  - `已落地统一响应、traceId 过滤器、全局异常与 CORS 配置。`
  - `已打通 GET /api/health 与前端展示，并实现 MySQL/Redis/ES/MinIO 连通探针聚合。`
  - `已完成 /login 登录页最小可用联调（可发起登录并反馈成功/失败状态）。`
  - `自动化通过：backend mvn -B test（Tests run: 19, Failures: 0, Errors: 0）+ mvn spring-boot:run（默认 dev profile，MySQL 启动成功）；frontend npm run build。`
  - `已完成 JWT 最小认证闭环（register/login/me）与 refresh/logout 接口，接入 Spring Security + JWT。`
  - `权限边界已生效：未鉴权拒绝 /api/admin/** 与 /api/ai/**，评论写接口要求登录。`
  - `已补齐 Nginx 配置（default/prod），验证 /api 反代与 SPA 回退可用。`
  - `已完成手工联调：docker compose 服务启动成功；健康探针四项全绿；register/login/me 主链路成功；未登录访问 /api/admin/projects 返回 401。`
- 未完成项：
  - `M1 尚未阶段验收（仍在推进内容展示模块）。`
- 阻塞项：
  - `无`
- 下一步：
  - `继续推进 M1：补齐剩余页面/导航与移动端回归，完成阶段验收与提交。`

## 6. 分块与恢复（只保留索引）

- **分块原则**：每回合只做阶段文档中 **1 个小任务**，并闭环到至少一个可验证点（测试/联调/页面）。
- **会话快照模板**：见 `docs/开发文档/00E-附件-快照与回滚模板.md`
- **安全回滚指令**：见 `docs/开发文档/00E-附件-快照与回滚模板.md`

---
## 7. 阶段计划细化的同步约束（执行入口口径）

- 默认收口只更新：当前阶段文档 + `99-阶段验收清单.md` + 本页“快速回填区”（状态变更时）。
- 若本轮计划细化涉及到后续阶段的依赖（接口/页面占位/数据源/契约），必须同步更新“其他阶段文档（仅依赖相关）”以保证口径一致。
- 且计划细化条目必须包含前后端双端计划（前端可用/后端可用/后端不可用前端降级提示），不能只写单端。
