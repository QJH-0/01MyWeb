# 00E 执行总控看板（唯一入口）

> 本文档是执行入口，不承担详细规范。  
> 详细规则仍以 `00C`、`00D`、`API文档与系统架构`、以及 `错误码与错误响应` 为准。

## 0. 模型开发起读顺序（固定）

1. 先看本页“当前阶段”。
2. 再读 `00C-最终压缩执行版（AI只读此文档先行）.md`（执行铁律）。
3. 再读“当前阶段文档”（`01~08` 仅 1 份）。
4. 仅当遇到不确定/冲突时，按需读取：`00D-全模块实现方案与类设计蓝图.md` + `API文档与系统架构.md`（以及 `错误码与错误响应.md` 等契约/错误响应口径文档）。
5. 结束前只更新：当前阶段文档 + `99-阶段验收清单.md` + 本页；若发生契约/接口冲突并需要回写修正，按 `00C` 第 2/7 节先改相关文档（可能包含 `00C`/`API`/`需求文档.md`/`tech-spec.md` 等），再进入代码实现。

## 1. 当前阶段

- 当前阶段：`M0`
- 当前目标：完成本阶段 DoD 并更新 `99-阶段验收清单.md`
- 当前状态：`进行中`
- 最后更新时间：`2026-03-26`

## 2. 本轮仅允许读取文档

1. `00C-最终压缩执行版（AI只读此文档先行）.md`
2. `00D-全模块实现方案与类设计蓝图.md`
3. `API文档与系统架构.md`
4. `错误码与错误响应.md`
5. 当前阶段文档（M0~M8 仅 1 个）
6. `99-阶段验收清单.md`

说明：`00D` / `API` / `错误码` 仅在“遇到不确定/冲突”时读取；否则只读当前阶段文档并按 `00C` 执行。

## 3. 本轮执行模板

- 阶段：`Mx`
- 目标：`一句话目标`
- 涉及模块：`backend/frontend/docs`
- 涉及接口：`/api/...`
- 风险点：`最多 3 条`
- 禁止事项：`不跨阶段、不改未声明契约、不跳过测试`

## 4. 阶段闸门（全部通过才可进入下一阶段）

- [ ] 代码实现完成（仅本阶段范围）
- [ ] 自动化通过（后端 `mvn test` / 前端 `npm run build`）
- [ ] 联调通过（主链路可走通）
- [ ] 文档已更新（阶段文档 + `99-阶段验收清单.md`）
- [ ] 无新增未记录风险

## 5. 快速回填区（每轮结束填写）

- 完成项：
  - ``
- 未完成项：
  - ``
- 阻塞项：
  - ``
- 下一步：
  - ``

## 6. Token 预算提示：分块执行协议（强制）

目标：把一次开发回合拆成“可在单次上下文内闭环”的小块，避免中途遗忘导致返工。

- 分块粒度：
  - 每回合只做 **1 个小任务**（来自阶段文档的 3~5 子任务拆分）。
  - 该小任务必须可闭环到至少一个可验证点（测试通过 / 接口可调通 / 页面可见）。
- 上下文上限触发条件（任一满足就必须收口）：
  - 已涉及文件过多、讨论跨度变大、或出现“同时在追 2 条以上主线”的迹象。
  - 还没跑测试但已经改了多处核心逻辑。
- 收口动作（固定顺序）：
  - 先跑本任务范围内的最小验证（至少：后端相关则 `mvn test` 的最小集合；前端相关则 `npm run build` 或对应最小验证）。
  - 再填写第 7 节“会话快照”（用于恢复）。
  - 最后再决定是否进入“检查点提交”（第 8 节）。

## 7. 状态恢复机制：会话快照（每回合必填）

用途：当开发中断（关闭 IDE/上下文耗尽/切到别的事）后，下一次只读 `00E + 00C + 当前阶段文档` 就能恢复到同一执行面。

复制粘贴模板（填完留在本节末尾，覆盖旧内容即可）：

- 快照时间：`YYYY-MM-DD HH:mm`
- 当前阶段：`Mx`
- 本回合任务名：`一句话（必须能映射到阶段文档的子任务）`
- 已完成（可验证证据）：
  - `测试/构建命令 + 结果（通过/失败 + 关键信息）`
  - `联调方式（curl/Swagger/前端页面路径）+ 结果`
- 关键改动面（文件/接口/配置，最多 10 条）：
  - `path/to/file`
  - `/api/...`
- 未完成（下一步 1~3 条，按顺序）：
  - `...`
- 风险/已知问题（最多 3 条）：
  - `...`

## 8. 回滚指令：安全回退到上一阶段/上一检查点

原则：**先有检查点，再谈回滚**。未提交的工作优先用工作区回退；已提交但未推送可 reset；已推送只能 revert。

- 回滚前必做（避免误删）：
  - `git status`（确认是否有未提交修改）
  - `git diff`（确认回滚范围）
- 场景 A：未提交（只在工作区/暂存区）
  - 丢弃未暂存修改：`git restore .`
  - 丢弃已暂存修改：`git restore --staged .` 后再 `git restore .`
  - 只回退某个文件：`git restore path/to/file`
- 场景 B：已提交但未推送（本地安全回退）
  - 回退到上一个提交：`git reset --hard HEAD~1`
  - 回退到任意检查点提交：`git reset --hard <commitSha>`
- 场景 C：已推送到远端（禁止改写历史）
  - 使用反向提交：`git revert <commitSha>`（需要时可多次 revert）
- 建议的“检查点提交”命名（便于定位回滚点）：
  - `chore(checkpoint): Mx <任务名> (tests: pass)`
