# 00E 执行总控看板（唯一入口）

> 本文档是执行入口，不承担详细规范。  
> 详细规则仍以 `00C`、`API文档与系统架构`、以及 `错误码与错误响应` 为准。

## 0. 模型开发起读顺序（固定，尽量少读）

1. 先看本页“当前阶段”。
2. 再读 `00C-最终压缩执行版（AI只读此文档先行）.md`（铁律）。
3. 再读“当前阶段文档”（`01~08` 仅 1 份）。
4. **仅当遇到不确定/冲突时**才按需补读：`API文档与系统架构.md` / `错误码与错误响应.md` / `tech-spec.md` / `需求文档.md`。
5. 收口时只更新：当前阶段文档 + `99-阶段验收清单.md` + 本页“快速回填区”。

## 1. 当前阶段

- 当前阶段：`M4`
- 当前目标：搜索模块（接口校验、outbox、ES 索引与前台搜索体验，含降级提示）
- 当前状态：`M3 已收口（见 99-M3 结论）；本阶段按 05-M4-M5 与 99-M4 执行`
- 最后更新时间：`2026-03-28`

## 2. 本轮“默认只读”文档（3 份）

1. 本页（`00E`）
2. `00C-最终压缩执行版（AI只读此文档先行）.md`
3. 当前阶段文档（M0~M8 仅 1 个）

说明：`API` / `错误码` / `tech-spec` / `需求文档` / `99` 都是 **按需读取**：只有在“出现不确定/冲突/准备验收收口”时才打开。

## 3. 本轮执行模板

- 阶段：`Mx`
- 目标：`一句话目标`
- 涉及模块：`backend/frontend/docs`
- 涉及接口：`/api/...`
- 风险点：`最多 3 条`
- 禁止事项：`不跨阶段、不改未声明契约、不跳过测试`

## 4. 阶段闸门（全部通过才可进入下一阶段）

- [ ] 代码实现完成（仅本阶段范围）
- [ ] 自动化通过（后端 `mvn -B test` + `mvn spring-boot:run` / 前端 `npm run build`）
- [ ] 联调通过（主链路可走通；**默认**用 Cursor **Playwright MCP**（`user-playwright`）对本地全栈执行关键路径，快照/截图/网络日志留证；细则见 `.cursor/agents/e2e-runner.md`）
- [ ] 前端具备后端不可用降级（默认数据渲染 + “后端连接失败”可见提示，写操作不得误报成功）
- [ ] 文档已更新（阶段文档 + `99-阶段验收清单.md`）
- [ ] 无新增未记录风险

### 4.1 全栈主链路验证（默认 Playwright MCP）

与「本地手工点浏览器」等价或替代：在 Cursor 中调用已启用的 **Playwright MCP**（`user-playwright`），对运行在 `localhost` 的前后端执行 `99` 中对应阶段的 E2E 步骤。留证：`browser_take_screenshot` 路径、`browser_snapshot` 摘要、`browser_network_requests` / `browser_console_messages` 导出，或 `.playwright-mcp/` 下日志。前置条件与原全栈联调相同（端口、`VITE_ADMIN_TOKEN` / `APP_ADMIN_TOKEN`、账号与 `docker compose` 依赖等）。执行说明：`.cursor/agents/e2e-runner.md`。

## 5. 快速回填区（每轮结束填写）

> 目的：让进度“可审计、可追溯、可持续填写”。  
> 原则：**每轮只新增 1 条记录**（不改旧记录），并强制包含：本轮范围、证据、提交/未提交原因。

### 5.1 本轮回填（只填 1 条，固定格式）

| 字段 | 内容 |
| --- | --- |
| 轮次ID | `R-YYYYMMDD-xx`（当天从 01 递增） |
| 阶段/小任务 | `Mx / 阶段文档中的小任务标题（或编号）` |
| 本轮目标（一句话） | `...` |
| 范围（≤5条） | `frontend: ...`；`backend: ...`；`docs: ...` |
| 完成情况 | `完成/部分完成/未完成` + 一句话原因 |
| 验收证据（必填） | `mvn -B test: ...`；`mvn spring-boot:run: ...`；`npm run build: ...`；`Playwright MCP（或等价 E2E）: 步骤摘要 + 截图/快照/网络日志路径` |
| 风险/阻塞（≤3条） | `...` |
| Git 提交（必填其一） | `commit: <hash> <message>` 或 `未提交原因: ...（待提交范围：...；触发条件：...）` |
| 下一步（≤3条） | `...` |

### 5.2 进度日志（追加式，保留最近 10 条）

> 规则：每轮结束，把“5.1 本轮回填”复制为一条日志追加到此处；超过 10 条时，删除最旧的整条。

#### 最近进度

- `R-20260328-07`（M3 收口）
  - 阶段/小任务：`M3 / 评论写操作网络失败提示 + 99/计划/00E 阶段切换`
  - 完成情况：`完成：BlogCommentsPanel 对 Axios 无 response 时给出明确离线文案；全量 mvn -B test、npm test、npm run build；99-M3 功能项与结论勾选；00E/开发计划切换至 M4`
  - 验收证据：`mvn -B test: BUILD SUCCESS`；`npm test: 6 passed`；`npm run build: 成功`
  - 风险/阻塞：`tech-spec 3.1A Markdown 主源写回仍为专项外债务`
  - Git 提交：`face189 docs(m3): close M3 gate and switch to M4; fix(frontend): comment write error on network failure`
  - 下一步：`按 05-M4-M5 启动搜索 outbox/ES 与前台 /search 最小闭环`

- `R-20260328-06`（M3）
  - 阶段/小任务：`M3 / Playwright MCP 走查 + dev 库 Flyway V9（评论表）修复口径`
  - 完成情况：`完成：定位本地 `/api/comments` 500 根因（历史库无 V9 / Flyway 失败记录）；清理 comment 表与失败 V9 记录后由 Flyway 成功落 V9；全量 `mvn -B test`；MCP 验证 `/blog/1` 评论区与 `/admin/blogs`；勾选 99-M3 E2E 两项`
  - 验收证据：`mvn -B test: BUILD SUCCESS`；`npm test + npm run build: 通过`；`Playwright MCP: .playwright-mcp/m3-blog-detail-comments-2026-03-28.png、.playwright-mcp/m3-admin-blogs-2026-03-28.png`；`GET /api/comments: 200`
  - 风险/阻塞：`99-M3 功能项（CRUD/降级全文）仍部分未勾选；tech-spec 3.1A 主源写回未处理`
  - Git 提交：`docs(m3): Playwright E2E evidence and Flyway V9 dev notes`（以 `git log -1` 为准）
  - 下一步：`补齐 99 功能闸门勾选或 M3 结论收口；再评估 M4`

- `R-20260328-05`（M3）
  - 阶段/小任务：`M3 / 评论：项目详情页 targetType=project 挂载`
  - 完成情况：`完成：BlogCommentsPanel 泛化 props（targetType 默认 blog + targetId）；ProjectDetailView 引入面板；BlogDetailView 改用 targetId；frontend vitest + build；backend mvn -B test`
  - 验收证据：`mvn -B test: BUILD SUCCESS`；`npm test: passed`；`npm run build: 成功`；`关键路径: /projects/:id 评论区请求 targetType=project`
  - 风险/阻塞：`99-M3 E2E 与若干闸门仍待 MCP 留证后勾选`
  - Git 提交：`commit: 5c9a386 feat(m3): mount comments panel on project detail`
  - 下一步：`Playwright MCP 走查 99-M3；必要时收口 M3 文档勾选`

- `R-20260328-04`（M3）
  - 阶段/小任务：`M3 / 评论：后端 /api/comments + 前台详情评论区`
  - 完成情况：`完成：Flyway V9（comments/comment_likes）；CommentService（目标校验仅 PUBLISHED 博客/可见项目；点赞幂等切换）；CommentControllerIntegrationTest；前端 api/comments + BlogCommentsPanel（离线不拉取、写失败明确提示、登录跳转）`
  - 验收证据：`mvn -B test: BUILD SUCCESS`；`npm test: passed`；`npm run build: vite build 成功`；`关键路径: GET/POST /api/comments*；/blog/:id 评论区`
  - 风险/阻塞：`99-M3 结论区与手工验证项仍待逐条勾选；tech-spec 3.1A Markdown 主源写回未在本轮处理`
  - Git 提交：`feat(m3): add comments API and blog detail comment panel`（以 `git log -1` 为准）
  - 下一步：`手工走查 99-M3；必要时补项目详情页评论挂载（targetType=project）；再评估 M3 收口或 M4 入口`

- `R-20260328-03`（M3）
  - 阶段/小任务：`M3 / 前台 /blog 与 /admin/blogs 管理页对接`
  - 完成情况：`完成：api/blogs 客户端；BlogView/BlogDetailView（分类/标签/分页、详情 Markdown+DOMPurify、404 与离线降级分流）；BlogListView/BlogEditView（CRUD、发布/下架、错误码映射）；顶栏「博客后台」；Vitest 覆盖 renderSafeMarkdown`
  - 验收证据：`mvn -B test: BUILD SUCCESS`；`npm test: 2 passed`；`npm run build: vite build 成功`；`关键路径: /blog、/blog/:id、/admin/blogs*`
  - 风险/阻塞：`评论/点赞 API 未接入，详情页仅占位说明；M3 99 功能闸门仍待手工勾选`
  - Git 提交：`commit: 8663086 feat(m3): wire blog public and admin views to API`
  - 下一步：`接入 /api/comments 与登录态评论交互；全栈走查后勾选 99-M3 与阶段结论`

- `R-20260328-02`（M3）
  - 阶段/小任务：`M3 / 博客表结构与公开+管理端 API（含发布/下架）`
  - 完成情况：`完成：Flyway V8 blog/blog_tag；BlogService + BlogPublicController + AdminBlogController；草稿仅管理端可见、公开侧仅 PUBLISHED；slug 归一化与小写唯一；正文校验拒绝纯空白（含多行 Markdown）`
  - 验收证据：`mvn -B test: [SUCCESS] Tests run: 42, Failures: 0, Errors: 0`；`关键路径: POST/GET /api/admin/blogs*、GET /api/blogs、GET /api/blogs/slug/{slug}`
  - 风险/阻塞：`评论/Markdown XSS/搜索索引未在本轮接入；前台 BlogView 仍为占位需对接 API`
  - Git 提交：`feat(m3): add blog module backend with public and admin APIs`（以 `git log -1` 为准）
  - 下一步：`前端 /blog 列表/详情对接 + /admin/blogs 管理页；按需接入评论与 Markdown 净化策略`

- `R-20260328-01`（M2）
  - 阶段/小任务：`M2 / 管理端项目页错误提示与阶段文档同步`
  - 完成情况：`完成：管理端列表/编辑统一将 ApiResponse.error 业务码映射为用户可读文案；文档与 开发计划 同步至 M2 口径`
  - 验收证据：`mvn -B test: [SUCCESS] Tests run: 32, Failures: 0, Errors: 0`；`npm run build: [SUCCESS] vite build 完成`；`关键路径: /admin/projects 加载/删除失败时提示为映射文案而非原始 error 码`
  - 风险/阻塞：`tech-spec.md` 3.1A 要求的 Markdown 主源写回链路仍未接入，与本阶段 8A.2 表述存在差距，需在后续专项或 M3 前对齐口径。
  - Git 提交：`commit: 021e860 feat(m2): admin project API errors show user-friendly messages`
  - 下一步：`本地全栈联调走查；视结论勾选 99-M2 闸门或拆出「内容主源」专项`

- `R-20260327-02`（M2）
  - 阶段/小任务：`M2 / 项目模块后端接口与前台展示（含降级提示）`
  - 完成情况：`完成：后端项目 CRUD/查询接口 + 前端 projects 列表/详情接入，后台页面待后续补齐`
  - 验收证据：`mvn -B test: [SUCCESS] Tests run: 32, Failures: 0, Errors: 0`；`npm run build: [SUCCESS] vite build 完成`；`关键手工路径: /projects 列表可用，点进 /projects/:id 详情可用；后端不可用时降级渲染并提示“后端连接失败”`
  - 风险/阻塞：`无`
  - Git 提交：`commit: 62140be feat(m2): 项目模块后端接口与前台展示`
  - 下一步：`补齐后台项目管理页面（列表/新建/编辑/删除）并完成前后台联调，推进 M2 阶段验收`

- `R-20260327-01`（M1）
  - 阶段/小任务：`M1 / 首个可验收小任务闭环（含降级提示）`
  - 完成情况：`完成：M1 阶段验收通过，已推进至 M2`
  - 验收证据：`mvn -B test: Tests run: 28, Failures: 0, Errors: 0`；`npm run build: [SUCCESS] vite build 完成`；`关键手工路径: 直访 /about /experience /contact 刷新不 404；375px 导航折叠与跳转正常；后端不可用时页面降级渲染并提示“后端连接失败”`
  - 风险/阻塞：`无`
  - Git 提交：`commit: 9b9ff4e docs(m1): 完成验收收口并切换到 M2`
  - 下一步：`进入 M2：实现后台项目 CRUD 最小闭环 + 前台同步展示（含后端不可用降级提示）`

### 5.3 历史里程碑（仅大结果，禁止写成“每轮日志”）

- `M0`：已完成前后端工程初始化（Vue3+Vite+Router+Pinia / Spring Boot 3.5.12），统一响应/traceId/全局异常/CORS，健康检查聚合（MySQL/Redis/ES/MinIO），JWT 最小认证闭环与权限边界，Nginx 反代与 SPA 回退，docker compose 联调通过。

## 6. 分块与恢复（只保留索引）

- **分块原则**：每回合只做阶段文档中 **1 个小任务**，并闭环到至少一个可验证点（单元或集成测试 / **Playwright MCP** 全栈 E2E / 页面构建）。
- **会话快照模板**：见 `docs/开发文档/00E-附件-快照与回滚模板.md`
- **安全回滚指令**：见 `docs/开发文档/00E-附件-快照与回滚模板.md`

---
## 7. 阶段计划细化的同步约束（执行入口口径）

- 默认收口只更新：当前阶段文档 + `99-阶段验收清单.md` + 本页“快速回填区”（状态变更时）。
- 若本轮计划细化涉及到后续阶段的依赖（接口/页面占位/数据源/契约），必须同步更新“其他阶段文档（仅依赖相关）”以保证口径一致。
- 且计划细化条目必须包含前后端双端计划（前端可用/后端可用/后端不可用前端降级提示），不能只写单端。
