---
description: "项目统一执行总规则（精简整合版）"
alwaysApply: true
---
# 01MyWeb Core Rules

> 本文件为唯一默认生效的总规则；其他 `common-*` 与 `typescript-*` 规则作为参考模板使用（默认不自动生效）。

## 一、迭代方式（必须）

1. 不做“大爆发式”全量开发，一次只闭环一个主模块。
2. 永远先骨架后业务：M0 -> M1 -> M2 -> M3 -> M4/M5 -> M6 -> M7 -> M8。
3. 每个模块按固定流程执行：任务拆分 -> 实现 -> 自测 -> 联调 -> 回归 -> 文档标记 -> 提交。

## 二、测试与质量门禁（必须）

1. 新功能/缺陷修复默认走 TDD（RED -> GREEN -> REFACTOR）。
2. 最低覆盖要求：模块级 >= 80%（单元 + 集成；关键流程补 E2E）。
3. 未通过测试，不允许进入下一模块或进行提交。
4. 代码需满足：无静默吞错、边界输入已校验、无硬编码敏感信息。

## 三、安全与配置（必须）

1. 所有密钥仅允许环境变量注入，禁止入库。
2. 接口必须有参数校验和统一错误响应，避免泄露敏感内部信息。
3. 生产相关能力（HTTPS/鉴权/限流）在 M8 前完成最终收口验证。

## 四、文档与提交（必须）

1. 每完成一个功能点，必须同步更新 `开发计划.md` 与对应阶段/日程文档状态。
2. Git 原则：先测后提；提交信息遵循 Conventional Commits（feat/fix/refactor/docs/test/chore）。
3. 任一阶段 DoD 未通过，不得切入下一阶段。

## 五、执行优先级

当规则冲突时，按以下优先级执行：
1. 本文件
2. 当前任务明确要求
3. 其他参考规则