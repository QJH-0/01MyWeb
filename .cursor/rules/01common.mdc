---
description: "项目统一执行总规则（整合通用规则版）"
alwaysApply: true
---
# Core Rules

> 本文件为默认生效的通用总规则。
> 语言/框架专项规范应通过独立规则文件按 globs 生效。

## 一、迭代方式（必须）

1. 不做“大爆发式”全量开发，一次只闭环一个主模块。
2. 永远先骨架后业务：先最小可运行，再逐步补齐能力。
3. 每个模块按固定流程执行：任务拆分 -> 实现 -> 测试 -> 联调/回归 -> 文档更新 -> 提交。
4. 执行任务时默认不中途频繁暂停请求确认（如“要继续吗？”“确认修改吗？”），应持续推进并完整执行计划，直至 100% 完成；仅在用户明确要求暂停/确认或遇到不可自行决策的阻塞时再中断。

## 二、测试与质量门禁（必须）

1. 新功能/缺陷修复默认走 TDD（RED -> GREEN -> REFACTOR）。
2. 覆盖要求：模块级 >= 80%（Unit + Integration；关键流程补 E2E）。
3. 未通过测试，不允许进入下一模块或进行提交。
4. 代码需满足：无静默吞错、边界输入已校验、无硬编码敏感信息。
5. 测试金字塔：Unit 为主、Integration 保障边界、E2E 只覆盖关键路径；避免“冰淇淋锥”（过多 E2E）。
6. 失败排查：先用失败用例/日志证据定位，再修实现；保证测试隔离，避免相互污染。

## 三、安全与配置（必须）

1. 所有密钥仅允许环境变量注入，禁止入库。
2. 接口必须有参数校验和统一错误响应，避免泄露敏感内部信息。
3. 生产相关能力（HTTPS/鉴权/限流）必须在上线前完成最终收口验证。
4. 安全必检项（提交前）
   - 禁止硬编码 secrets（API keys、passwords、tokens）
   - 所有用户输入验证
   - 防 SQL 注入：使用参数化查询
   - 防 XSS：对可渲染 HTML 做清理/转义
   - 确保 CSRF 防护
   - 鉴权/授权已验证
   - 所有端点具备限流
   - 错误信息不泄露敏感细节
5. Secret 管理：禁止硬编码；缺失配置需 fail fast；疑似泄露需立即轮换并回查。
6. 日志安全：禁止记录密码、token、敏感个人信息；防日志注入；保证日志访问权限与留存策略。

## 四、文档与提交（必须）

1. 每完成一个功能点，必须同步更新对应计划与文档状态。
2. Git 原则：先测后提；提交信息遵循 Conventional Commits。
3. 每完成一个“已通过测试且可验收”的功能点，必须在当前回合立即执行 Git 提交；禁止把多个已完成功能长期堆在未提交状态。
4. 若存在“明确不提交”的用户指令，必须在回合输出中显式记录“未提交原因”和“待提交范围”，避免遗漏。
5. 任一阶段/里程碑 DoD 未通过，不得切入下一阶段/里程碑。

## 五、执行优先级

当规则冲突时，按以下优先级执行：
1. 本文件
2. 当前任务明确要求
3. 其他参考规则

## 六、知识来源与检索策略（必须）

1. 默认不使用模型训练数据作为首选依据，尤其避免直接采用可能过时的代码写法。
2. 涉及库/API/框架用法、版本差异、配置项、报错排查时，优先通过联网检索与官方文档确认。
3. 优先使用 Context7 获取最新文档与示例；无法覆盖时再补充其他可靠网络来源并交叉验证。

提交信息格式：`<type>[scope]: <description>`（必要时在 footer 标注 breaking change）。

## 七、编码规范（Coding Style）

1. 不可变（CRITICAL）
   - ALWAYS 创建新对象，NEVER 原地修改（避免隐藏副作用）
2. 遵循流行规范（CRITICAL）
   - 前后端实现常见能力时优先采用官方/社区推荐的主流写法与约定（框架文档优先）。
   - 不为解决常见问题“自己创造”新的约定/协议/中间件/错误格式；必要时复用项目既有模式与统一约定。
3. 文件组织
   - MANY SMALL FILES > FEW LARGE FILES
   - 常见规模：200-400 行，最大不超过 800 行
   - 从大模块提取 utilities
   - 按 feature/domain 组织，而不是按类型组织
4. 错误处理
   - 每一层都显式处理错误
   - UI 面向代码：提供用户友好的错误信息
   - 服务端：记录详细错误上下文
   - 禁止静默吞错
5. 输入校验（边界处校验）
   - 所有用户输入必须先校验再处理
   - 有 schema 验证能力优先用 schema（如 Zod；后端以 Java Bean Validation 为准）
   - Fail fast + 清晰的错误信息
   - 不信任外部数据（API/用户输入/文件内容等）
6. 完成前检查表
   - 可读性：命名合理、结构清晰
   - 函数粒度：小而专注（<50 行为宜）
   - 文件聚焦：<800 行
   - 嵌套深度不过深（>4 层避免）
   - 错误处理完整
   - 无硬编码值（使用常量或配置）
   - 使用不可变模式（避免 mutation）

## 八、工程模式与代码习惯（Patterns）

1. 遵循流行规范（CRITICAL）
   - 优先采用官方/社区主流、可维护的工程模式与约定（先复用，后扩展）。
   - 不为常见问题“自己创造”新的模式/协议/错误格式/响应格式；如需自定义，必须有明确收益与一致性方案。
2. 常见可复用模式（如适用）
   - Repository：业务依赖抽象接口，存储细节下沉到实现层，便于测试与替换
   - API 响应 envelope：成功/失败字段与分页元数据口径统一（`total/page/limit`）

## 九、Hooks 系统（Hooks）

1. Hook 仅保留必要能力：参数校验、自动格式化、会话结束前最终检查
2. 自动授权仅在可信任务中开启，禁止使用高风险跳过权限配置
3. 多步骤任务需维护 `TodoWrite`，保证步骤可追踪且不漏项

## 十、性能优化与排错（Performance）

1. Context 窗口管理
   - 避免使用上下文的最后 20% 用于大规模重构/跨多文件实现/复杂交互调试
   - 低上下文敏感任务：单文件编辑、独立工具创建、文档更新、小型 bug fix
2. Extended Thinking + Plan Mode
   - 复杂任务先做结构化方案，再进入实现
   - 多方案有取舍时，先对比再落地
3. 构建失败排错
   - 优先修复构建/测试阻断，再继续功能迭代
   - 按项目技术栈运行对应的构建与测试命令，逐步定位并修复（例如：前端 build/test；后端 unit/integration test）

## 十一、Git 工作流（Git Workflow）

1. PR 工作流（创建 PR 时）
   - 分析完整 commit history（不要只看最新提交）
   - 用 `git diff [base-branch]...HEAD` 预览差异
   - PR 摘要要完整、清晰
   - 附上测试计划（TODO）
   - 新分支推送时使用 `-u` 标志

## 十二、特性开发流程（Development Workflow）

1. Plan First
   - 先输出实现计划
   - 识别依赖与风险，拆分阶段
2. TDD Approach
   - 写测试优先（RED）
   - 实现通过测试（GREEN）
   - 重构（IMPROVE）
   - 验证覆盖率（80%+）
3. 代码审查
   - 代码写完后立即进行代码审查（质量/安全/可维护性）
   - 优先修复 CRITICAL/HIGH 问题
   - 其他 Medium 问题尽量修
4. Commit & Push
   - 提交信息按本文件“四、文档与提交”规范

## 十三、Agent 协作编排（Agent Orchestration）

1. 何时直接调用（无需用户提示）
   - 复杂功能：先做实现规划（plan），再进入实现
   - 代码写完：进行代码审查（code review）
   - 缺陷修复/新功能：采用 TDD（test-first）
   - 架构决策：先做架构评审与权衡
   - 构建失败：先修复构建/测试，再继续开发
   - 变更已通过测试且准备提交：执行标准化提交流程
2. 并行任务执行
   - 使用并行执行处理彼此独立的工作
3. 多视角分析
   - 复杂问题拆分子角色：事实审阅、资深工程、专家安全、一致性审阅、冗余检查等

## 十四、前端设计技能与 UI 规范（必须）

1. 前端任务默认调用 `frontend-design` skill
   - 适用范围：页面、组件、布局、样式、交互、可视化与前端重构
   - 若任务明确要求其它设计/实现方式，以用户要求优先
2. 所有前端产出必须遵循项目 UI 规范，禁止风格漂移
   - 优先复用项目既有设计系统（颜色、字号、间距、圆角、阴影、动效）
   - 优先复用已有组件与样式约定，不重复造轮子
   - 新增视觉风格前，需与现有页面保持整体一致性（品牌、可读性、可访问性）
   - 若存在已定义视觉规范或 design token，前端实现必须以该规范为准
3. 交付前必须执行前端一致性自检
   - 检查主题/配色一致、排版层级一致、状态样式完整（hover/active/disabled/error）
   - 检查响应式与可访问性（键盘可达、语义结构、对比度）是否达标

## 十五、通用代码风格（精简版）

1. 命名：类/类型 `PascalCase`；方法/变量 `lowerCamelCase`；常量 `UPPER_SNAKE_CASE`；测试以 `Test` 结尾
2. 导入：Java 禁止通配符导入；TypeScript 禁止未使用导入，保持分组与可读性
3. 结构与格式：保持 UTF-8、去行尾多余空白；遵循模板元素顺序（package/import/单一顶层类等）
4. 错误处理：对外返回通用错误信息/错误码；服务端通过日志记录详细上下文并包含 `traceId`，避免泄露堆栈/SQL/路径
5. 生产限制：生产代码禁止无审计的 `console.log` 或直接打印堆栈

参考（原则来源）：
- Google Java Style：https://google.github.io/styleguide/javaguide.html
- OWASP 错误处理：https://cheatsheetseries.owasp.org/cheatsheets/Error_Handling_Cheat_Sheet.html
 - OWASP Logging：https://cheatsheetseries.owasp.org/cheatsheets/Logging_Cheat_Sheet.html
 - Conventional Commits：https://www.conventionalcommits.org/