---
description: "项目统一执行总规则（整合通用规则版）"
alwaysApply: true
---
# 01MyWeb Core Rules

> 本文件为唯一默认生效的总规则（已整合原 `common-*` 通用细则）。
> 前端 TypeScript/JavaScript 专项规则见：`typescript.mdc`；后端 Java/Spring 专项规则见：`java.mdc`（均按 globs 生效）。

## 一、迭代方式（必须）

1. 不做“大爆发式”全量开发，一次只闭环一个主模块。
2. 永远先骨架后业务：M0 -> M1 -> M2 -> M3 -> M4/M5 -> M6 -> M7 -> M8。
3. 每个模块按固定流程执行：任务拆分 -> 实现 -> 自测 -> 联调 -> 回归 -> 文档标记 -> git提交。
4. 执行任务时默认不中途频繁暂停请求确认（如“要继续吗？”“确认修改吗？”），应持续推进并完整执行计划，直至 100% 完成；仅在用户明确要求暂停/确认或遇到不可自行决策的阻塞时再中断。

## 二、测试与质量门禁（必须）

1. 新功能/缺陷修复默认走 TDD（RED -> GREEN -> REFACTOR）。
2. 最低覆盖要求：模块级 >= 80%（单元 + 集成；关键流程补 E2E）。
3. 未通过测试，不允许进入下一模块或进行提交。
4. 代码需满足：无静默吞错、边界输入已校验、无硬编码敏感信息。
5. 测试类型（全部需要）
   - Unit Tests：单个函数/工具/组件
   - Integration Tests：API endpoints、数据库操作
   - E2E Tests：关键用户流程（E2E 框架按语言选择）
6. TDD 工作流（MANDATORY）
   - 先写测试（RED）
   - 运行测试：应失败
   - 写最小实现（GREEN）
   - 再运行测试：应通过
   - 重构（REFACTOR）
   - 校验覆盖率（80%+）
7. 测试失败排查（先实现后修正）
   - 用 `tdd-guide` agent 辅助定位失败原因
   - 检查测试隔离性（避免相互污染）
   - 核对 mocks/stubs 是否正确
   - 优先修实现（除非测试本身写错）

## 三、安全与配置（必须）

1. 所有密钥仅允许环境变量注入，禁止入库。
2. 接口必须有参数校验和统一错误响应，避免泄露敏感内部信息。
3. 生产相关能力（HTTPS/鉴权/限流）在 M8 前完成最终收口验证。
4. 安全必检项（提交前）
   - 禁止硬编码 secrets（API keys、passwords、tokens）
   - 所有用户输入验证
   - 防 SQL 注入：使用参数化查询
   - 防 XSS：对可渲染 HTML 做清理/转义
   - 确保 CSRF 防护
   - 鉴权/授权已验证
   - 所有端点具备限流
   - 错误信息不泄露敏感细节
5. Secret 管理
   - NEVER 在源码硬编码 secrets
   - ALWAYS 使用环境变量或密钥管理器
   - 启动时验证必需 secrets 是否存在
   - 对可能已泄露的 secrets 做轮换
6. 安全应急协议（发现问题）
   - 立即停止（STOP）
   - 使用 `security-reviewer` agent
   - 关键安全问题先修（CRITICAL before continue）
   - 轮换任何已暴露 secrets
   - 回查代码库中类似问题

## 四、文档与提交（必须）

1. 每完成一个功能点，必须同步更新 `开发计划.md` 与对应阶段文档状态。
2. Git 原则：先测后提；提交信息遵循 Conventional Commits（feat/fix/refactor/docs/test/chore/perf/ci）。
3. 任一阶段 DoD 未通过，不得切入下一阶段。

## 五、执行优先级

当规则冲突时，按以下优先级执行：
1. 本文件
2. 当前任务明确要求
3. 其他参考规则

## 六、知识来源与检索策略（必须）

1. 默认不使用模型训练数据作为首选依据，尤其避免直接采用可能过时的代码写法。
2. 涉及库/API/框架用法、版本差异、配置项、报错排查时，优先通过联网检索与官方文档确认。
3. 优先使用 Context7 获取最新文档与示例；无法覆盖时再补充其他可靠网络来源并交叉验证。

补充：提交信息格式（推荐）

```
<type>: <description>

<optional body>
```

常用 type：`feat`、`fix`、`refactor`、`docs`、`test`、`chore`、`perf`、`ci`

## 七、编码规范（Coding Style）

1. 不可变（CRITICAL）
   - ALWAYS 创建新对象，NEVER 原地修改（避免隐藏副作用）
2. 文件组织
   - MANY SMALL FILES > FEW LARGE FILES
   - 常见规模：200-400 行，最大不超过 800 行
   - 从大模块提取 utilities
   - 按 feature/domain 组织，而不是按类型组织
3. 错误处理
   - 每一层都显式处理错误
   - UI 面向代码：提供用户友好的错误信息
   - 服务端：记录详细错误上下文
   - 禁止静默吞错
4. 输入校验（边界处校验）
   - 所有用户输入必须先校验再处理
   - 有 schema 验证能力优先用 schema（如 Zod；后端以 Java Bean Validation 为准）
   - Fail fast + 清晰的错误信息
   - 不信任外部数据（API/用户输入/文件内容等）
5. 完成前检查表
   - 可读性：命名合理、结构清晰
   - 函数粒度：小而专注（<50 行为宜）
   - 文件聚焦：<800 行
   - 嵌套深度不过深（>4 层避免）
   - 错误处理完整
   - 无硬编码值（使用常量或配置）
   - 使用不可变模式（避免 mutation）

## 八、工程模式与代码习惯（Patterns）

1. Skeleton 项目
   - 优先找成熟 skeleton 项目
   - 用并行 agents 做评估：安全性、可扩展性、相关性评分、实现规划
   - 基于最匹配的 skeleton 作为起点再迭代
2. Repository Pattern
   - 将数据访问封装到统一接口层
   - 标准操作：`findAll`、`findById`、`create`、`update`、`delete`
   - 具体实现处理存储细节（数据库/API/文件等）
   - 业务逻辑依赖抽象接口，而非具体存储机制
   - 提升可替换性与可测试性（可 mock）
3. API 响应格式（统一 envelope）
   - success/status 指示
   - 数据载荷：成功时为数据，失败时可为 null
   - 错误信息字段：成功时可为 null
   - 分页元数据：total/page/limit

## 九、Hooks 系统（Hooks）

1. Hook 类型
   - `PreToolUse`：工具执行前（校验参数、修改参数等）
   - `PostToolUse`：工具执行后（自动格式化/校验等）
   - `Stop`：会话结束（最终验证）
2. 自动接收权限（谨慎使用）
   - 仅在可信且计划明确时开启
   - 不要使用 `dangerously-skip-permissions`
   - `allowedTools` 通过 `~/.claude.json` 配置
3. `TodoWrite` 最佳实践
   - 用它来记录多步任务的进度与关键理解点
   - 避免漏写步骤、避免颗粒度错误导致返工
   - Todo 列表也能帮助发现步骤顺序问题

## 十、性能优化与排错（Performance）

1. 模型选择策略（按任务类型）
   - `Haiku 4.5`：轻量 agent、频繁调用、并行 worker
   - `Sonnet 4.6`：主要编码与编排多 agent 工作流
   - `Opus 4.5`：复杂架构决策、最高推理需求、研究分析
2. Context 窗口管理
   - 避免使用上下文的最后 20% 用于大规模重构/跨多文件实现/复杂交互调试
   - 低上下文敏感任务：单文件编辑、独立工具创建、文档更新、小型 bug fix
3. Extended Thinking + Plan Mode
   - Extended thinking 默认开启（预留内部 reasoning token）
   - 需要结构化方案时启用 Plan Mode
4. 构建失败排错
   - 用 `build-error-resolver` agent
   - 按技术栈选择命令逐步修复并验证（前端：`npm run build`；后端：`mvn -B test` / `mvn spring-boot:run`）

## 十一、Git 工作流（Git Workflow）

1. PR 工作流（创建 PR 时）
   - 分析完整 commit history（不要只看最新提交）
   - 用 `git diff [base-branch]...HEAD` 预览差异
   - PR 摘要要完整、清晰
   - 附上测试计划（TODO）
   - 新分支推送时使用 `-u` 标志

## 十二、特性开发流程（Development Workflow）

1. Plan First
   - 用 `planner` agent 输出实现计划
   - 识别依赖与风险，拆分阶段
2. TDD Approach
   - 用 `tdd-guide` agent
   - 写测试优先（RED）
   - 实现通过测试（GREEN）
   - 重构（IMPROVE）
   - 验证覆盖率（80%+）
3. 代码审查
   - 代码写完后立刻用 `code-reviewer`（通用审查）
   - 优先修复 CRITICAL/HIGH 问题
   - 其他 Medium 问题尽量修
4. Commit & Push
   - 提交信息按本文件“四、文档与提交”规范

## 十三、Agent 协作编排（Agent Orchestration）

1. 可用 agents（位于 `.cursor/agents/`）
   - planner：实现规划
   - architect：系统设计
   - tdd-guide：TDD（新功能/缺陷修复）
   - code-reviewer：通用代码审查（跨语言/跨层）
   - frontend-reviewer：Vue3 前端专项代码审查（涉及前端改动时）
   - java-reviewer：Java/Spring 后端专项代码审查
   - security-reviewer：安全分析（提交前）
   - build-error-resolver：构建错误修复
   - java-build-resolver：Java/Maven 构建错误专项修复
   - e2e-runner：E2E 测试（关键流程）
   - refactor-cleaner：清理无用代码
   - doc-updater：文档更新
2. 何时直接调用（无需用户提示）
   - 复杂功能：`planner`
   - 刚修改/刚写完代码：`code-reviewer`
   - 涉及前端 Vue3 改动：补 `frontend-reviewer`
   - 涉及后端 Java/Spring：补 `java-reviewer`
   - 涉及后端构建失败：优先 `java-build-resolver`
   - 缺陷修复/新功能：`tdd-guide`
   - 架构决策：`architect`
3. 并行任务执行
   - 使用并行 agents 处理彼此独立的工作
4. 多视角分析
   - 复杂问题拆分子角色：事实审阅、资深工程、专家安全、一致性审阅、冗余检查等