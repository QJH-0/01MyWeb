---
description: "Java/Spring 后端专项规则（01MyWeb）"
globs:
  - "**/*.java"
  - "**/pom.xml"
  - "**/application*.yml"
  - "**/application*.yaml"
alwaysApply: false
---

# Java/Spring 专项规则

> 本文件用于 01MyWeb 后端（Spring Boot 3.5.x + Java 21 + Maven）。

## 一、代码与分层规范

1. 严格分层：`controller` / `service` / `repository` / `dto` / `vo` / `config` / `common`
2. Controller 只做参数接收与响应封装，不承载复杂业务逻辑
3. Service 负责业务编排与事务边界
4. Repository 仅做数据访问，不写业务规则
5. 统一响应体与错误处理，返回中包含 `traceId`

## 二、接口与安全

1. 所有对外接口必须做参数校验（`@Valid` + 约束注解）
2. 错误响应禁止暴露堆栈、SQL、内部路径等敏感细节
3. Secrets 只允许环境变量注入，禁止硬编码
4. 与 GitHub/DashScope/MinIO 等外部服务交互时，必须做超时、重试、降级
5. 对写操作接口补充鉴权和限流策略（按阶段落地）

## 三、数据库与检索

1. SQL 使用参数化查询，禁止字符串拼接
2. 涉及 MySQL + Elasticsearch 一致性的写流程按规范执行（允许最终一致，不允许静默失败）
3. 关键查询必须评估索引命中和 N+1 风险
4. 变更数据模型时同步更新迁移脚本与文档状态

## 四、测试与构建

1. 新增/修改后端能力默认遵循 TDD（RED -> GREEN -> REFACTOR）
2. 后端最小验证命令：
   - `mvn -B test`
   - 需要联调时：`mvn spring-boot:run`
3. 失败处理原则：先修复构建/测试阻断，再继续功能迭代

## 五、Agent 协作建议（后端）

1. 后端代码审查：`java-reviewer`
2. Java/Maven 构建问题：`java-build-resolver`
3. 涉及 SQL / schema / 索引：`database-reviewer`
4. 涉及安全输入边界：`security-reviewer`

## 六、Java 风格（精简版）

1. 命名：类/接口/枚举 `UpperCamelCase`；方法/变量 `lowerCamelCase`；常量 `UPPER_SNAKE_CASE`；测试以 `Test` 结尾
2. 导入：禁止通配符导入（`import ...*`），显式 import
3. 文件：UTF-8；去行尾多余空白；遵循模板元素顺序（package/import/单一顶层类）

参考：Google Java Style：https://google.github.io/styleguide/javaguide.html

